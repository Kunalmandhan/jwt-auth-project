# Copy this to .env and fill in your values
# In production, set these as environment variables instead

# JWT Configuration
JWT_SECRET=your_super_secret_key_here_minimum_32_characters
JWT_EXPIRATION=86400000

# Database (for production - replace H2 with a real DB)
# DB_URL=jdbc:postgresql://localhost:5432/jwtdb
# DB_USERNAME=your_db_user
# DB_PASSWORD=your_db_password
